import {Scenes} from './Libs.js'

const Sliders = {
    
}

export default Sliders


/*
use it like showSlider(sliderName)
like: d, v, r -> only those sliders are visible

and for all visible call
Sliders.showAll()
*/